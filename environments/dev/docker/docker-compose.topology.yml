# Auto-generated by scripts/lib/gen_topology.py — do not edit manually.
# Source of truth: config/topology.json

networks:
  bubble-nextcloud:
    driver: bridge
    ipam:
      config:
        - subnet: "10.20.1.0/24"

services:

  ansible:
    networks:
      bubble-nextcloud:
        ipv4_address: "10.20.1.2"

  checkmk:
    networks:
      bubble-nextcloud:
        ipv4_address: "10.20.1.3"

  jumphost:
    networks:
      bubble-nextcloud:
        ipv4_address: "10.20.1.10"

  # ── Bubble: nextcloud (10.20.1.0/24) ────────────────────────────────────────

  nextcloud-app:
    build:
      context: ./build/workload
      dockerfile: Dockerfile
    container_name: "dev-local-srv-nextcloud-app"
    hostname: "dev-local-srv-nextcloud-app.localhost"
    networks:
      bubble-nextcloud:
        ipv4_address: "10.20.1.11"
    environment:
      PUPPET_SERVER: "10.10.0.21"
      CHECKMK_SERVER: "10.10.0.23"
      PULP_SERVER: "10.10.0.24"
    dns: ["10.10.0.30"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "22"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    ports:
      - "127.0.0.1:40443:443"
      - "127.0.0.1:40080:80"

  nextcloud-db:
    build:
      context: ./build/workload
      dockerfile: Dockerfile
    container_name: "dev-local-srv-nextcloud-db"
    hostname: "dev-local-srv-nextcloud-db.localhost"
    networks:
      bubble-nextcloud:
        ipv4_address: "10.20.1.12"
    environment:
      PUPPET_SERVER: "10.10.0.21"
      CHECKMK_SERVER: "10.10.0.23"
      PULP_SERVER: "10.10.0.24"
    dns: ["10.10.0.30"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "22"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  nextcloud-redis:
    build:
      context: ./build/workload
      dockerfile: Dockerfile
    container_name: "dev-local-srv-nextcloud-redis"
    hostname: "dev-local-srv-nextcloud-redis.localhost"
    networks:
      bubble-nextcloud:
        ipv4_address: "10.20.1.13"
    environment:
      PUPPET_SERVER: "10.10.0.21"
      CHECKMK_SERVER: "10.10.0.23"
      PULP_SERVER: "10.10.0.24"
    dns: ["10.10.0.30"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "22"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
